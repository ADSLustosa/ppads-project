{
    "version": "0.2.0",
    "configurations": [
        {
        "name": "Flask (python -m flask)",
        "type": "python",
        "request": "launch",
        "module": "flask",
        "args": ["run"],
        "envFile": "${workspaceFolder}/.env",
        "jinja": true
        }
    ]
}
